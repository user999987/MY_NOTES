'''

for i in range(array):
    for j in range(0,i)
        if array[j]>array[i]
            greater+=1
    for j in range(i+1,len(array)):
        if array[j]<array[i]
            smaller+=1
    result+=smaller*greater

'''